/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn.dribbble.com",
      "assets.aceternity.com",
      "images.unsplash.com",
      "aceternity.com",
    ],
  },
};

export default nextConfig;
